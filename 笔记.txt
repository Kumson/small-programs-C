1.main函数必须返回整数，即只有“int main()”
2.定义的函数使用前需要声明，就像使用的变量一样
3.switch语句必须有“break;”作用是跳出循环
4.C语言中%是取余运算，不是取模
5.把字符串拆成一个个字母：定义数组a[x]， scanf("%s",a)
未写入字符的第一位为'\0' (编码为0的字符)
后面的数组位相当于被删掉了
6. % 取模运算是一个重要的数学工具
7.for语句有循环结构中前后操作对应关系清晰的特点，
while循环缩进后不清晰
8.各种操作（赋值等）只能在函数中进行.
9.“%.xf”输出x位小数（四舍五入），注意“.”
10.“%”不能对float类数使用，其他运算符可以对任何算数类型使用
11.y = x ++ 和y =  ++ x代表不同的赋值和运算顺序
12.continue语句是结束当次循环进入下一次
13.Dev中bool即是逻辑型变量，在<stdio.h>中
14.转义字符后带数字是8进制，加x是16进制
15.乘法的实现是通过移位，如*7=*4+*2+*1